This program (trigmon) will monitor the triggers live in memory to see if a quake was detected
by QCN participant machines (either the "normal" BOINC project or the
"continual" project).

Dependencies:  mysql


